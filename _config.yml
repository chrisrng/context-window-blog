# Site settings
title: context window – the blog
description: Chris Ng's blog about technical leadership, software architecture, and software development.
baseurl: "/context-window-blog" # the subpath of your site, e.g. /blog
url: "https://chrisrng.github.io" # the base hostname & protocol for your site

# Build settings
markdown: kramdown

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

# Plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag
